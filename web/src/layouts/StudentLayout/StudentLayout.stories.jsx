import StudentLayout from './StudentLayout'

export const generated = (args) => {
  return <StudentLayout {...args} />
}

export default {
  title: 'Layouts/StudentLayout',
  component: StudentLayout,
}
