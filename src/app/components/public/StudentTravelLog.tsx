import { useState, useEffect } from 'react';
import { MapPin, Clock, X } from 'lucide-react';
import { Enrollment, Location } from '@generated/prisma';

export const StudentTravelLog = ({ enrollments, groupLocations }: { enrollments: Enrollment[], groupLocations: Location[] }) => {
  // const [students, setStudents] = useState(enrollments);

  return (
    <div>hi</div>
  )
};